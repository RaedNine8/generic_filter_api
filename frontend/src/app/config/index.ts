/**
 * Configuration Module Index
 *
 * This module contains all entity configurations.
 * Each entity has its own config file defining fields, columns, quick filters, etc.
 */

export * from "./entities";
